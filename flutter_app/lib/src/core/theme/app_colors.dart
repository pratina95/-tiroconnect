import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const primary = Color(0xFF1E88E5); // Blue
  static const primaryDark = Color(0xFF1565C0);
  static const primaryLight = Color(0xFF42A5F5);

  // Secondary Colors
  static const secondary = Color(0xFF4CAF50); // Green
  static const secondaryDark = Color(0xFF388E3C);
  static const secondaryLight = Color(0xFF81C784);

  // Accent Colors
  static const accent = Color(0xFFFF9800); // Orange
  static const accentDark = Color(0xFFF57C00);

  // Status Colors
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFF9800);
  static const error = Color(0xFFF44336);
  static const info = Color(0xFF2196F3);

  // Light Theme Colors
  static const backgroundLight = Color(0xFFF5F7FA);
  static const surfaceLight = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const border = Color(0xFFE0E0E0);

  // Dark Theme Colors
  static const backgroundDark = Color(0xFF121212);
  static const surfaceDark = Color(0xFF1E1E1E);
  static const textPrimaryDark = Color(0xFFE0E0E0);
  static const textSecondaryDark = Color(0xFFA0A0A0);
  static const borderDark = Color(0xFF333333);

  // Rating Colors
  static const ratingGold = Color(0xFFFFD700);
  static const ratingGrey = Color(0xFFBDBDBD);

  // Verification Badge
  static const verifiedBlue = Color(0xFF2196F3);

  // Premium Colors
  static const premium = Color(0xFF9C27B0); // Purple
}
